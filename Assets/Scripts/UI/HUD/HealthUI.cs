using Microsoft.Unity.VisualStudio.Editor;
using UnityEngine;

public class HealthUI : MonoBehaviour
{
    //holds mask images
    [SerializeField] Image[] images;

    public void UpdateHealth(int currHealth, int maxHealth)
    {
        //enable only the necessary amount of images

        //hide the 
    }
}
